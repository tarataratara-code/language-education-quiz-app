rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/apps/{appId} {
      allow read, write: if request.auth != null
        && request.auth.uid == userId
        && appId == "language-education-quiz-app";
    }
  }
}
